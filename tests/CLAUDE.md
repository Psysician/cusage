# CLAUDE.md

## Overview

This directory contains the regression suites, fixture assets, and helpers that exercise the packaged `cusage` CLI.

## Index

| File | Contents (WHAT) | Read When (WHEN) |
| --- | --- | --- |
| `CLAUDE.md` | Directory index and test-area routing | Orienting in `tests/` or choosing the right test surface |
| `codex-family-aggregation.test.mjs` | Codex family de-overlap aggregation regressions | Changing Codex session lineage or merge behavior |
| `e2e/CLAUDE.md` | Packaged-CLI end-to-end suite index | Adding or debugging fixture-backed CLI regressions |
| `fast-mode.test.mjs` | Fast-mode cache and refresh gating regressions | Modifying cache-first refresh behavior |
| `fixtures/CLAUDE.md` | Fixture scenario catalog and golden asset index | Updating deterministic scenarios, cache seeds, or goldens |
| `pricing.test.mjs` | Pricing table and service-tier regressions | Changing token pricing or tier multipliers |
| `provider-flags.test.mjs` | Provider flag and alias regressions | Modifying CLI provider filtering or aliases |
| `support/CLAUDE.md` | Shared harness helper index | Adjusting fixture execution, isolation, or output normalization |
